// mips-specific (big endian) vet whitelist. See readme.txt for details.

// Work around if-def'd code. Will be fixed by golang.org/issue/17544.
runtime/sys_linux_mipsx.s: [mips] walltime: invalid MOVW of sec+0(FP); int64 is 8-byte value containing sec_hi+0(FP) and sec_lo+4(FP)
runtime/sys_linux_mipsx.s: [mips] nanotime: invalid MOVW of ret+0(FP); int64 is 8-byte value containing ret_hi+0(FP) and ret_lo+4(FP)
